[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "ase-geometry-rs"
version = "0.1.0"
description = "Rust-accelerated geometry routines for ASE"
requires-python = ">=3.9"

[tool.maturin]
module-name = "ase._geometry_rs"
features = ["pyo3/extension-module"]
